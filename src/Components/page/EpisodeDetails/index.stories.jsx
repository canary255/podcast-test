import React from "react";
import { EpisodeDetails } from ".";

export default {
  title: "Page/EpisodeDetails",
  component: EpisodeDetails,
  argTypes: {},
};

const Template = () => <EpisodeDetails />;

export const Default = Template.bind({});
Default.args = {};
